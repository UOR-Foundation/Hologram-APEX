//! Character Table of the Monster Group

use crate::foundation::Exact;

/// Character table
pub struct CharacterTable {
    /// Character values (exact)
    pub values: Vec<Vec<Exact>>,
}

// Full implementation deferred to Phase 1
